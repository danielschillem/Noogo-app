# DigitalOcean — backend et frontend sur le même domaine
# → URLs relatives (pas de CORS, pas de domaine à hardcoder)
VITE_API_URL=/api
VITE_IMAGE_BASE_URL=
# Pusher — temps réel (laisser vide pour désactiver)
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=eu
